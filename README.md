# World Tests

Проект: **интерактивный сайт с тестами** + **мировой рейтинг** (leaderboard) на **Next.js (Vercel)** + **Supabase (Postgres)**.

## Что уже есть
- 🔐 **Регистрация через Email** — простая регистрация с указанием имени, фамилии и Telegram ника
- 📝 Страница `/test` — демо‑тест (вопросы на фронте, ответы/проверка — на сервере)
- 🏆 Страница `/leaderboard` — общий рейтинг (по сумме очков) с именами, фамилиями и кликабельными Telegram аккаунтами
- 👤 Страница `/profile` — личный кабинет пользователя с историей пройденных тестов
- 📊 **Живой рейтинг** — обновление рейтинга в реальном времени на главной странице
- API:
  - `POST /api/auth/register` — регистрация нового пользователя
  - `POST /api/submit` — отправка результата теста (начисление очков)
  - `GET /api/leaderboard` — топ рейтинга (JSON)
  - `GET /api/user/profile` — профиль пользователя
  - `GET /api/user/attempts` — история попыток пользователя

> Важно: ключ Supabase **service role** используется ТОЛЬКО на сервере (в API/Server Components). На клиент он не утекает.

---

## Быстрый старт локально

### 1) Установить зависимости
```bash
npm i
```

### 2) Создать Supabase проект и применить SQL
1. Создай проект в Supabase
2. Открой **SQL editor**
3. Вставь и выполни: `supabase/schema.sql`

### 3) Настроить переменные окружения
Создай файл `.env.local` в корне проекта:

```bash
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here
```

### 4) Запуск
```bash
npm run dev
```

Открой: http://localhost:3000

---

## Деплой на Vercel

1) Залей проект в GitHub/GitLab
2) Импортируй репозиторий в Vercel
3) В настройках проекта (Settings → Environment Variables) добавь env vars:
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

4) Deploy

---

## Где что менять

- Тест: `src/tests/test-1.public.ts` (что видит пользователь)
- Ответы (сервер): `src/tests/test-1.answer.ts` (не импортировать в клиент!)
- Формула очков: `src/app/api/submit/route.ts`
- UI страниц: `src/app/*`
- SQL схема: `supabase/schema.sql`

---

## Функционал регистрации

При первом посещении сайта пользователь видит модальное окно с формой регистрации:
- **Email** (обязательно, уникальный)
- **Имя** (обязательно, 1-50 символов)
- **Фамилия** (обязательно, 1-50 символов)
- **Ник в Telegram** (необязательно, для связи с участниками)

После регистрации пользователь попадает на главную страницу с:
- Живым рейтингом слева (обновляется каждые 10 секунд)
- Доступными тестами справа
- Иконкой личного кабинета в верхнем правом углу

В рейтинге отображается:
- Имя и фамилия
- Email
- **Telegram ник (кликабельный)** — ссылка на `https://t.me/@username` для связи с участником

---

## Следующие шаги (когда будешь расширять)
- Добавить подтверждение email при регистрации
- Хранить тесты в базе (questions публично, ответы — только сервер/приватно)
- Сезоны, категории, античит, лимиты попыток, “лучший результат в зачёт” и т.д.
