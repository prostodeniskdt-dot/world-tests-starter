import "server-only";

/**
 * ВНИМАНИЕ:
 * Этот файл нельзя импортировать в клиентские компоненты,
 * иначе правильные ответы утекут в браузер.
 */

export const CARBONIZATION_PRACTICE_2_SECRET = {
  id: "carbonization-practice-2",
  basePoints: 200,
  difficulty: 2,
  maxAttempts: null as number | null,
  answerKey: {
    q1: 1, // B
    q2: 0, // A
    q3: [0,2], // [1]=A, [2]=C
    q4: [0,1,2], // A, B, C
    q5: 1, // Примеси/осадок (индекс 0) соответствует эффекту с индексом 1
    q6: {answer: false, reason: 0}, // False - важно давление равновесия
    q7: 0, // Да, избыточная пена может указывать на перекарбонизацию
    q8: [0,3], // A, D
    q9: 0, // A
    q10: [0,1], // Ошибки: id=1 и id=2 (индексы 0,1)
    q11: 0, // Проверить температуру → Оценить ABV → Проверить чистоту → Проверить перекарбонизацию
    q12: 0, // A
    q13: 0, // Контролируемо снизить уровень CO₂
    q14: [1,2], // Промывка чайных листьев, пектиназа (методы снижения пеностабилизаторов)
    q15: 0, // A
    q16: 0, // A
    q17: [0,1], // [1]=A, [2]=B
    q18: [0,1,2] // A, B, C
  } as Record<string, any>,
};
