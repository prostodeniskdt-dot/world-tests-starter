# Руководство по переносу тестов из baza.txt на сайт

## 🎯 Общие принципы

Эта инструкция описывает **точный формат** для записи тестов в файл `baza.txt`, а также правила его автоматического парсинга и переноса на сайт.

**КРИТИЧЕСКИ ВАЖНО:** Строго соблюдайте форматы, описанные ниже. Даже мелкие отклонения могут привести к ошибкам парсинга.

---

## 📄 Структура файла baza.txt

### Заголовок теста

**Формат:**
```
ТЕСТ 1 — «Три штриха баланса»
```
или
```
ТЕСТ 3 (Сложный) — «Инженерия вкуса»
```

**Правила:**
- Номер теста: `ТЕСТ 1`, `ТЕСТ 2`, `ТЕСТ 3`
- Уровень сложности (опционально в скобках): `(Простой)`, `(Средний)`, `(Сложный)`
- Если уровень не указан, определяется по номеру: 1 = Простой, 2 = Средний, 3 = Сложный
- Название в кавычках: `«...»` или `"..."`

---

### Структура вопроса

```
Вопрос X. Текст вопроса? (Тип механики)
[Опции или элементы вопроса]
Правильный ответ: [формат ответа]
Пояснение: Текст пояснения для показа после ответа
```

**Альтернативный формат (для заданий):**
```
Задание X. Текст задания. (Тип механики)
[Элементы задания]
Ответ: [формат ответа]
Пояснение: Текст пояснения
```

---

## 🧩 Типы механик вопросов и их форматы

### 1. **Multiple Choice (Single select)** — Одиночный выбор

**Формат в baza.txt:**
```
Вопрос 1. Какой правильный ответ? (Single select)
A) Первый вариант
B) Второй вариант
C) Третий вариант
D) Четвертый вариант
Правильный ответ: A
Пояснение: Объяснение правильного ответа.
```

**Формат ответа в ключе:**
```
1A
```
где `A` = индекс 0, `B` = индекс 1, `C` = индекс 2, `D` = индекс 3

---

### 2. **Multiple Select** — Множественный выбор

**Формат в baza.txt:**
```
Вопрос 6. Какие утверждения верны? (Multiple select) (Верных: 2)
A) Первое утверждение
B) Второе утверждение
C) Третье утверждение
D) Четвертое утверждение
E) Пятое утверждение
F) Шестое утверждение
Правильный ответ: A, D
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
6A,D
6(A,D)
6 A,D
```

**В answer.ts конвертируется в:**
```typescript
q6: [0, 3]  // A=0, D=3
```

---

### 3. **Matching** — Сопоставление

**Формат в baza.txt:**
```
Задание 2. Соотнесите компонент и его роль. (Matching)
Слева:
1. Сахар
2. Биттер
3. Виски (основа)
4. Цедра цитрусов
Справа:
A) Подчеркивает пряность и связывает резкость
B) Добавляет аромат масла
C) Дает доминирующий аромат
D) Придает аромат малыми дозами
Ответ: 1–A, 2–D, 3–C, 4–B
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
2(1A2D3C4B)           // компактный формат
2(1–A,2–D,3–C,4–B)    // с тире и запятыми
2: 1–A,2–D,3–C,4–B    // с двоеточием
```

**В answer.ts конвертируется в:**
```typescript
q2: [[0,0],[1,3],[2,2],[3,1]]  // пары [leftIndex, rightIndex]
```

**ВАЖНО:** 
- Индексы в ключе 1-based (1,2,3,4), в TypeScript 0-based (0,1,2,3)
- Буквы A,B,C,D → индексы 0,1,2,3

---

### 4. **Ordering** — Упорядочивание

**Формат в baza.txt:**
```
Задание 4. Расставьте шаги в логичном порядке. (Ordering)
Элементы:
A) Первый шаг
B) Второй шаг
C) Третий шаг
D) Четвертый шаг
Ответ: A → B → C → D
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
4(A→B→C→D)
4 A→B→C→D
```

**В answer.ts конвертируется в:**
```typescript
q4: [0,1,2,3]  // порядок индексов
```

---

### 5. **True/False Enhanced** — Верно/Неверно с причиной

**Формат в baza.txt:**
```
Вопрос 5. Утверждение: «Текст утверждения». (True/False + Reason)
   1. Верно/Неверно: Неверно
   2. Выберите причину:
A) Первая причина
B) Вторая причина
C) Третья причина
D) Четвертая причина
Ответ: Неверно; причина: A
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
5Неверно/A
5(Неверно;A)
5 Неверно/C? (A)
```

**В answer.ts конвертируется в:**
```typescript
q5: {answer: false, reason: 0}  // false/true, A=0
```

---

### 6. **Grouping/Classification** — Группировка

**Формат в baza.txt:**
```
Задание 8. Разнесите утверждения по категориям. (Grouping/Classification)
Категории: «Усиление основы», «Смещение баланса»
Элементы:
A) Добавить намёк сладости
B) Переусердствовать с биттером
C) Выбрать основу правильно
D) Добавить слишком много сахара
E) Дать слишком мало биттера
Ответ: «Усиление основы»: A, C; «Смещение баланса»: B, D, E
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
8(A,C / B,D,E)
8 У: A,C / С: B,D,E
```

**В answer.ts конвертируется в:**
```typescript
q8: {"Усиление основы":[0,2], "Смещение баланса":[1,3,4]}
```

**ВАЖНО:** 
- Категории должны быть перечислены в строке "Категории:" в том же порядке, что и в ответе
- Разделитель между категориями: `/` или `;`

---

### 7. **Cloze Dropdown** — Заполнение пропусков

**Формат в baza.txt:**
```
Задание 10. Заполните пропуск, выбрав вариант. (Dropdown cloze)
Текст: «Сладость в напитке важна не только ради вкуса: она также добавляет [1: ___]».
Варианты для [1]:
A) тело и густоту
B) кислотность и резкость
C) пузырьки и шипение
D) солёность и сухость
Ответ: [1]=A
Пояснение: Объяснение.
```

**Для нескольких пропусков:**
```
Текст: «Сироп [1: ___] и иногда добавляют [2: ___]».
Варианты для [1]:
A) вариант 1A
B) вариант 1B
...
Варианты для [2]:
A) вариант 2A
B) вариант 2B
...
Ответ: [1]=A; [2]=A
```

**Форматы ответа в ключе:**
```
10([1]=A)
10[1A]         // компактный формат
10 [1]=A
5[1A,2A]       // для нескольких пропусков
```

**В answer.ts конвертируется в:**
```typescript
q10: [0]        // для одного пропуска
q5: [0,0]       // для двух пропусков
```

---

### 8. **Select Errors** — Выбор ошибок

**Формат в baza.txt:**
```
Задание 16. Найдите 2 ошибки в объяснении (выберите 2 номера). (Select errors)
Текст:
[1] «Напиток держится на крепкой основе».
[2] «Если сахара мало, биттер всё равно доминирует».
[3] «Если биттера слишком много, профиль распадается».
[4] «Сироп удобнее сахара».
[5] «Лучше всегда увеличивать сахар».
[6] «Цедра добавляет аромат масла».
Ответ: 2, 5
Пояснение: Объяснение ошибок.
```

**Форматы ответа в ключе:**
```
16(2,5)
16 2,5
```

**В answer.ts конвертируется в:**
```typescript
q16: [1,4]  // 2→1, 5→4 (1-based to 0-based)
```

**ВАЖНО:** Номера в ключе 1-based, конвертируются в 0-based индексы!

---

### 9. **Two-Step** — Двухшаговый вопрос

**Формат в baza.txt:**
```
Задание 10. Двухступенчатое: корректировка при использовании вермута. (Two-step / branching)
Задание 10 (Часть 1). Выберите наиболее корректное действие:
A) Уменьшить подсластитель
B) Увеличить подсластитель
C) Уменьшить оба компонента
D) Увеличить оба компонента
Задание 10 (Часть 2). Выберите объяснение к выбранному действию:
A) Вермут уже сладкий
B) Вермут кислый
C) Вермут газирован
D) Вермут крепче виски
Ответ: Ч1=A; Ч2=A
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
10(Ч1A Ч2A)
10(Ч1=A;Ч2=A)
10 Ч1=A;Ч2=A
```

**В answer.ts конвертируется в:**
```typescript
q10: {step1: 0, step2Mapping: {0: 0, 1: 1, 2: 2, 3: 3}}
```

**ВАЖНО:** step2Mapping указывает правильный ответ для step2 в зависимости от выбора в step1. В большинстве случаев это простое соответствие {0:0, 1:1, 2:2, 3:3}.

---

### 10. **Matrix** — Матрица соответствий

**Формат в baza.txt:**
```
Задание 12. Для каждого случая выберите эффект. (Grid / matrix)
Объекты:
1. Увеличили сахар
2. Увеличили биттер
3. Уменьшили биттер
Характеристики:
A) Профиль глохнет
B) Лекарственный оттенок
C) Превращается в виски со льдом
D) Появляется кислотность
Ответ: 1–A; 2–B; 3–C
Пояснение: Объяснение.
```

**Форматы ответа в ключе:**
```
12(1A2B3C)           // компактный
12(1–A;2–B;3–C)      // с разделителями
12 1–A;2–B;3–C       // без скобок
```

**В answer.ts конвертируется в:**
```typescript
q12: {0: 0, 1: 1, 2: 2}  // {rowIndex: columnIndex}
```

---

### 11. **Scenario** — Ситуационный вопрос

**Формат в baza.txt:**
```
Задание 1. Ситуация: гость любит крепко, но жалуется. Что делать? (Scenario-based mini-case)
A) Сохранить крепкую основу
B) Снизить крепость
C) Убрать биттер
D) Увеличить сахар резко
Правильный ответ: A
Пояснение: Объяснение.
```

**Формат ответа в ключе:**
```
1A
```

**В answer.ts конвертируется в:**
```typescript
q1: 0  // индекс правильного действия
```

---

## 📝 Формат ключа ответов

В конце каждого теста должна быть строка с ключом:

```
Ключ (Тест 1):
1A; 2: 1–A,2–D,3–C,4–B; 3A; 4 A→B→C→D; 5 Неверно/A; 6 A,D; 7A; 8 У: A,C / С: B,D,E; 9A; 10 [1]=A; 11A; 12 Верно/A; 13A; 14 1–B,2–C,3–A,4–D; 15A; 16 2,5; 17A; 18A; 19 D→C→A→B; 20A.
```

**ВАЖНО:**
- Разделяйте ответы точкой с запятой `;` или запятой `,`
- Для вопросов с несколькими ответами (multiple select) используйте запятую БЕЗ пробела между буквами или формат `(A,D)`
- Номер вопроса всегда идет первым

---

## 🔧 Автоматический парсинг и генерация

### Шаги для автоматического переноса:

1. **Подготовьте файл baza.txt** в правильном формате (см. выше)

2. **Запустите скрипт парсинга:**
```bash
npm run parse-baza
```

3. **Скрипт автоматически:**
   - Парсит все тесты из файла
   - Определяет типы механик вопросов
   - Конвертирует ответы в правильный формат TypeScript
   - Генерирует файлы `public.ts` и `answer.ts` для каждого теста
   - Создает директории в `src/tests/`

4. **Проверьте результаты:**
   - Откройте сгенерированные файлы в `src/tests/[test-id]/`
   - Убедитесь, что ответы корректно конвертированы
   - Проверьте, что нет `null` вместо правильных ответов

---

## ⚠️ Частые ошибки и как их избежать

### Ошибка 1: Все ответы = null

**Причина:** Формат ключа ответов не распознан парсером.

**Решение:**
- Проверьте, что ключ начинается с `Ключ (Тест X):`
- Убедитесь, что номера вопросов соответствуют реальным
- Проверьте разделители между ответами (`;` или `,`)

**Правильно:**
```
Ключ (Тест 1):
1A; 2(1A2D3C4B); 3A; 4(A→B→C→D).
```

**Неправильно:**
```
Ответы:  ← неправильное слово
1-A, 2-(1A2D3C4B)  ← неправильный формат
```

---

### Ошибка 2: Неправильная конвертация индексов

**Причина:** Смешивание 1-based и 0-based индексации.

**Решение:**
- В файле baza всегда используйте 1-based (1, 2, 3, 4 для Слева; A, B, C, D для Справа)
- Парсер автоматически конвертирует в 0-based для TypeScript
- Не пытайтесь вручную корректировать индексы

---

### Ошибка 3: Пропущены элементы в вопросах

**Причина:** Неправильная структура вопроса (нет секций "Слева:", "Справа:", "Элементы:")

**Решение:**
- Для Matching всегда указывайте `Слева:` и `Справа:`
- Для Ordering/Grouping указывайте `Элементы:`
- Для Cloze указывайте `Текст:` с пропусками `[1: ___]`
- Для Matrix указывайте `Объекты:` и `Характеристики:`

---

### Ошибка 4: Неправильный тип механики

**Причина:** Тип указан неправильно или не указан вообще.

**Решение:** Используйте ТОЧНЫЕ названия типов:
- `(Single select)` — для multiple-choice
- `(Multiple select)` — для множественного выбора
- `(Matching)` — для сопоставления
- `(Ordering)` — для упорядочивания
- `(Grouping/Classification)` — для группировки
- `(True/False + Reason)` — для верно/неверно с причиной
- `(Dropdown cloze)` — для заполнения пропусков
- `(Select errors)` — для выбора ошибок
- `(Two-step / branching)` — для двухшаговых
- `(Grid / matrix)` — для матрицы
- `(Scenario-based mini-case)` — для ситуационных

---

### Ошибка 5: Проблемы с кодировкой

**Причина:** Использование неправильных символов тире/кавычек.

**Решение:**
- Используйте обычное тире `–` (en dash) или дефис `-` для пар в matching: `1–A` или `1-A`
- Используйте стрелку `→` для ordering: `A→B→C→D`
- Используйте кавычки-ёлочки `«»` или обычные `""`
- Не используйте экзотические Unicode символы

---

## 📚 Справочная таблица форматов ответов

| Тип механики | Пример ключа | Пример в answer.ts |
|-------------|-------------|-------------------|
| multiple-choice | `1A` | `q1: 0` |
| multiple-select | `6A,D` или `6(A,D)` | `q6: [0,3]` |
| matching | `2(1A2D3C4B)` | `q2: [[0,0],[1,3],[2,2],[3,1]]` |
| ordering | `4(A→B→C→D)` | `q4: [0,1,2,3]` |
| grouping | `8(A,C / B,D,E)` | `q8: {"Кат1":[0,2], "Кат2":[1,3,4]}` |
| true-false-enhanced | `5Неверно/A` | `q5: {answer: false, reason: 0}` |
| cloze-dropdown | `10[1A]` | `q10: [0]` |
| select-errors | `16(2,5)` | `q16: [1,4]` (1-based → 0-based)|
| two-step | `10(Ч1A;Ч2A)` | `q10: {step1: 0, step2Mapping: {...}}` |
| matrix | `12(1A2B3C)` | `q12: {0:0, 1:1, 2:2}` |
| scenario | `1A` | `q1: 0` |

---

## 🤖 Инструкция для AI-агента

**Когда пользователь пишет:** 
> "Загрузил обновленный файл baza, перенеси тесты на сайт"

**Выполни следующие шаги:**

1. **Прочитай файл** `src/tests/baza.txt`

2. **Проверь структуру:**
   - Есть ли заголовки тестов в правильном формате?
   - Есть ли ключ ответов в конце каждого теста?
   - Все ли вопросы имеют правильные типы механик?

3. **Запусти автоматический парсинг:**
```bash
npm run parse-baza
```

4. **Проверь результаты:**
   - Открой сгенерированные файлы в `src/tests/[test-id]/answer.ts`
   - Убедись, что НЕТ вопросов с `null` в качестве ответа
   - Если есть `null` — сообщи пользователю о проблеме

5. **Обнови tests-registry.ts:**
   - Добавь импорты новых тестов
   - Добавь их в массивы `PUBLIC_TESTS` и `SECRET_TESTS`

6. **Протестируй:**
   - Запусти тестовый сервер
   - Открой новый тест в браузере
   - Пройди тест и проверь, что ответы проверяются правильно

7. **Закоммить изменения:**
```bash
git add src/tests/
git commit -m "Добавлены новые тесты из baza.txt"
git push
```

---

## 📞 Поддержка

Если возникли проблемы с парсингом:
1. Проверьте формат файла baza по этой инструкции
2. Убедитесь, что используются правильные разделители
3. Проверьте, что ключ ответов в конце теста
4. Запустите парсер с флагом verbose для детальной информации

---

**Версия документа:** 1.0  
**Дата создания:** 2026-01-16  
**Последнее обновление:** 2026-01-16
