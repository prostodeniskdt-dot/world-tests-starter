# Инструкция по работе с файлом baza.txt

## 📋 Обязательная процедура перед парсингом

Перед запуском парсинга файла `baza.txt` **ВСЕГДА** выполняйте следующие шаги:

### 1. Проверка формата файла (ВАЛИДАЦИЯ)

**Выполните:**
```bash
npm run validate-baza
```

**Ожидаемый результат:**
- ✅ Все ошибки должны быть исправлены
- ⚠️ Предупреждения можно игнорировать, если они не критичны

**Если валидация не прошла:**
1. Исправьте все ошибки, указанные в выводе
2. Проверьте наличие ключей ответов для всех тестов
3. Убедитесь, что формат ключей корректен: `**Ключ (Тест X):** 1A, 2B, ...`
4. Повторно запустите валидацию

### 2. Запуск парсинга

**Только после успешной валидации выполните:**
```bash
npm run parse-baza
```

## 🔍 Что проверяет валидация

Валидация проверяет:

1. **Наличие ключей ответов** — для каждого теста должен быть ключ в формате `**Ключ (Тест X):**`
2. **Соответствие тестов и ключей** — количество ключей должно соответствовать количеству тестов
3. **Формат ключей** — проверяется базовый формат ответов (1A, 2B, etc.)
4. **Количество ответов** — предупреждение, если количество ответов меньше половины от количества вопросов
5. **Структура заголовков** — проверка наличия уровня сложности и названия теста

## 📝 Формат файла baza.txt

### Структура теста

```
ТЕСТ 1 (Простой) — «Название теста»

**1) multiple-choice**
Текст вопроса?
A) Вариант 1
B) Вариант 2
C) Вариант 3
D) Вариант 4
**Ответ:** A
**Пояснение:** Объяснение ответа...

**2) multiple-select**
Текст вопроса?
A) Вариант 1
B) Вариант 2
C) Вариант 3
**Ответ:** A, B
**Пояснение:** Объяснение...

---

**Ключ (Тест 1):**
1–A; 2–A,B; 3–(ответ1, ответ2); ...
```

### Обязательные элементы

1. **Заголовок теста:**
   - Формат: `ТЕСТ X (Уровень) — «Название»`
   - Уровни: `Простой`, `Средний`, `Сложный`

2. **Ключ ответов:**
   - Обязательно в конце каждого теста
   - Формат: `**Ключ (Тест X):** 1A, 2B, ...`
   - Должен содержать ответы для всех вопросов

3. **Ответы в вопросах:**
   - Формат: `**Ответ:** A` или `**Ответ:** A, B`
   - Могут быть во встроенном виде, но ключ обязателен

## 🚨 Частые ошибки

### Ошибка: "Тест X не имеет ключа ответов"
**Решение:** Добавьте строку `**Ключ (Тест X):** 1A, 2B, ...` после последнего вопроса теста.

### Ошибка: "Найден ключ для теста X, но сам тест не найден"
**Решение:** Проверьте номер теста в ключе — он должен соответствовать номеру в заголовке.

### Предупреждение: "В ключе найдено меньше ответов, чем вопросов"
**Решение:** Проверьте, что все ответы указаны в ключе. Убедитесь, что формат ответов корректен.

### Предупреждение: "Ключ может иметь нестандартный формат"
**Решение:** Проверьте формат ответов в ключе. Используйте стандартный формат:
- Простой выбор: `1A`
- Множественный выбор: `2A,B` или `2(A,B)`
- Сопоставление: `3(1–A,2–B)`
- Упорядочивание: `4(A→B→C)`

## 💡 Инструкция для агента

Когда пользователь просит обработать файл `baza.txt`, выполните следующие действия:

1. **ВСЕГДА сначала запускайте валидацию:**
   ```
   Запускаю валидацию файла baza.txt...
   [запустить: npm run validate-baza]
   ```

2. **Анализируйте результаты валидации:**
   - Если есть ошибки — сообщите пользователю и попросите исправить
   - Если есть предупреждения — проанализируйте их важность
   - Если валидация прошла успешно — продолжайте

3. **Только после успешной валидации запускайте парсинг:**
   ```
   Валидация прошла успешно. Запускаю парсинг...
   [запустить: npm run parse-baza]
   ```

4. **Анализируйте вывод парсинга:**
   - Проверьте логи парсинга на наличие предупреждений
   - Если есть вопросы без ответов — сообщите пользователю
   - Если парсинг завершился успешно — подтвердите

## 📌 Пример правильной последовательности команд

```bash
# Шаг 1: Валидация
npm run validate-baza

# Если валидация успешна:
# Шаг 2: Парсинг
npm run parse-baza

# Если валидация не прошла:
# Исправьте ошибки в файле baza.txt
# Повторите валидацию
```

## ⚠️ Важно

- **НЕ запускайте парсинг без валидации** — это может привести к некорректным результатам
- **ВСЕГДА проверяйте вывод валидации** — даже предупреждения могут указывать на проблемы
- **Исправляйте ошибки перед парсингом** — парсер может пропустить некоторые проблемы, но они проявятся позже
